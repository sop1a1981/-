# 아로마스 블로그 프로젝트 규칙

> ## ⛔ 파이프라인 에이전트는 이 문서를 따르지 마세요
>
> **Step 0~7 에이전트(PM·SEO·라이터·텍스트마이닝·QC·카드뉴스·디자이너)는
> 자신의 `agents/step_XX_*.md` 시스템 프롬프트만 따릅니다.**
> 이 문서의 출력 형식 규칙은 **메인 세션이 사람에게 채팅으로 보여줄 때**만
> 적용됩니다.
>
> 특히 **라이터(Step 2)는 이미지 프롬프트를 만들지 않습니다.** 본문 원고만
> 쓰고, 사진이 들어갈 자리는 `[이미지: 설명]` 한 줄로 표시하면 끝입니다.
> 영문 이미지 생성 프롬프트는 나중에 메인 세션이 따로 만듭니다.
>
> *(2026-09-05 실제 사고: 라이터가 이 문서를 읽고 본문 대신 이미지 프롬프트를
> 출력해 파이프라인이 중단됐다.)*

## 포스팅 결과를 채팅에 출력할 때

아래 규칙의 적용 주체는 **메인 세션**이다. 파이프라인 산출물을 받아
사람에게 보여줄 때 이 형식으로 정리한다.

파이프라인으로 생성한 포스팅을 사용자에게 보여줄 때는 **항상** 아래 형식을
지킨다. 매 포스팅마다 예외 없이 적용한다.

### 1. 제목과 본문을 각각 코드블록으로

**제목을 먼저 코드블록 하나로 따로 출력한다.** 네이버 에디터의 제목 칸에
바로 붙일 수 있어야 한다. 추천 제목 하나를 맨 위에 두고, 대안 후보는
그 아래 별도 코드블록에 담는다.

이어서 본문 전체를 하나의 코드블록에 담는다. 네이버 에디터에 그대로 복사해
붙일 수 있어야 하므로 마크다운 강조 기호(`**`, `#`)를 넣지 않는다.

⚠️ 첫 번째 제목 후보는 본문 첫 줄(훅)로도 쓰인다. **제목을 바꾸면 본문
첫 줄도 같이 바꿔서 두 코드블록이 어긋나지 않게 한다.** 썸네일에 넣은
문구도 함께 맞춘다.

### 2. 이미지 프롬프트를 코드블록으로 따로

본문과 **분리해서** 출력한다. 이미지마다 코드블록 하나씩,
GPT 이미지 생성에 그대로 붙여 넣을 수 있는 완성된 프롬프트로 쓴다.

**프롬프트는 한글로 작성한다.** 영문으로 쓰지 않는다.

| 용도 | 크기 | 텍스트 |
|---|---|---|
| 썸네일 | **800 x 800** | **정중앙에 포스팅 제목** |
| 본문 이미지 | **1600 x 900** | 없음 |

- **썸네일은 반드시 따로 생성한다.** 본문 이미지에서 잘라 쓰지 않는다
- **썸네일 정중앙에 그 포스팅의 제목을 넣는다.** 제목 코드블록에 쓴 문구와
  글자 하나까지 같아야 한다
- 본문 이미지는 원고의 `[이미지: 설명]` 자리마다 하나씩

### 2-1. 인물과 배경은 한국

- 등장 인물은 **한국인**으로 명시한다 (연령대·성별도 함께)
- 배경도 **한국 환경**으로 지정한다 — 한국 가정집, 한국 사무실,
  한국 정형외과 진료실 등
- 서양식 인테리어나 외국인 모델이 나오지 않게 프롬프트에 못박는다

### 3. 한글 텍스트가 들어가는 이미지

텍스트 오버레이가 있으면 프롬프트에 **한글을 선명하고 정확하게
렌더링하라는 지시를 반드시 포함한다.** 예:

```
[텍스트 렌더링 필수 조건]
위에 따옴표로 적은 한글을 글자 하나도 바꾸지 말고 그대로 렌더링할 것.
모든 한글 음절이 또렷하고 선명하게, 획이 뭉개지거나 번지지 않게.
철자를 틀리거나 없는 글자를 지어내거나 비슷한 글자로 바꾸지 말 것.
글자는 픽셀 단위로 선명하고 배경과 대비가 뚜렷해야 함.
```

실제 넣을 문구는 따옴표로 정확히 명시한다.

### 4. 동작·자세가 있는 이미지

"굽은 자세" 같은 뭉뚱그린 표현을 쓰지 않는다. **관절 각도, 신체 부위의
위치 관계, 시선 방향까지** 구체적으로 서술한다. 예:

- ❌ `bad posture at a desk`
- ✅ `head jutted 6-7cm forward of the shoulder line, chin tilted slightly
  up, upper back rounded into a C-curve, shoulders rolled forward`

잘못된 자세와 바른 자세를 대비하는 이미지라면 **양쪽 모두** 같은 수준으로
구체적으로 묘사한다.

### 5. 브랜드 톤

이미지가 브랜드와 따로 놀지 않게 한다.

- 컬러: 베이지 `#F5ECD7`, 딥 오렌지 `#D4550A`, 크림 화이트 `#FBF7F0`,
  다크 브라운 `#3D1F0D`, 미디엄 베이지 `#C9A97A`
- 톤: 따뜻한 조명, 차분하고 전문적, 과한 연출 없음
- 의료 시술처럼 보이지 않게 한다 (의료법)

## 자세·동작·운동은 근거를 대조한 뒤 쓴다

포스팅과 이미지 프롬프트에 들어가는 자세·동작·스트레칭은 **웹 검색으로
대조한 뒤** 확정한다. 참고 순서:

1. 해부학·역학 근거 (systematic review, 임상 가이드)
2. 재활·운동 전문 사이트 (NASM 등)
3. 운동 블로그·SNS — **현재 실제로 많이 쓰이는 방법**을 파악하는 용도

운동·스트레칭은 **지금 널리 실행되는 방법을 우선**한다. 교과서에만 있고
현장에서 안 쓰는 동작을 넣지 않는다.

### ⚠️ 파이프라인 에이전트는 검색을 못 한다

`agents.py`의 `_call()`이 `allowed_tools=[]`로 호출하므로 Step 0~7
에이전트는 웹에 접근할 수 없다. **검증은 메인 세션이 한다** — 파이프라인
산출물을 사용자에게 보여주기 전에 자세·동작·운동 서술을 대조하고,
틀렸거나 낡았으면 고친 뒤 무엇을 왜 바꿨는지 함께 알린다.

### 검증된 참고 기준 (2026-08 확인)

**상부교차증후군(Upper Crossed Syndrome)** — 거북목·라운드숄더·굽은등의
표준 설명 틀. 아로마스 콘텐츠의 목·어깨 주제는 대체로 여기에 걸린다.

- 짧아지고 과활성: 가슴근(대흉근·소흉근), 상부승모근, 견갑거근, 후두하근
- 늘어나고 약화: 심부 목굴곡근, 중·하부 승모근, 능형근, 전거근
- 방치 시 진행: 경추성 두통, 흉곽출구증후군, 어깨충돌증후군
- ⚠️ **UCS는 동시에 나타나는 불균형 패턴이지, 정해진 순서로 진행하는
  단계가 아니다.** "1단계 → 2단계" 식으로 단정하지 말고 "이런 순서로
  나타나는 경우가 많다" 정도로 완화해서 쓴다

**현재 널리 쓰이는 교정 동작** (거북목·라운드숄더 계열)

| 동작 | 목적 |
|---|---|
| 턱 당기기 (chin tuck / 경추 후인) | 심부 목굴곡근 강화 — 가장 기본 |
| 견갑골 모으기 (scapular retraction) | 능형근·중부승모근 활성 |
| 가슴 스트레칭 (도어웨이 pec stretch) | 짧아진 대·소흉근 이완 |
| 흉추 신전 (thoracic extension) | 굽은 등 가동성 확보 |

이 네 가지가 현재 표준 조합이다. 스트레칭 주제 포스팅은 여기서 출발한다.

### 이미지에 적용할 때

- **바른 자세**: 귀가 어깨 관절 바로 위, 턱은 살짝 당김, 경추의 자연스러운
  전만 곡선 유지, 견갑골은 아래·뒤로, 흉추는 편 상태
- **잘못된 자세**: 귀가 어깨보다 앞, 턱 내밀고 살짝 들림, 뒷목 압박,
  흉추 C자 굴곡, 어깨 앞으로 말림
- **라운드숄더 관찰 지표**: 견갑골이 척추에서 멀어져 벌어지고, 팔이 내회전해
  **손등이 뒤를 향한다**

## 파이프라인 실행 전

`git status`로 워킹 트리를 비운다. 커밋되지 않은 변경이 있으면 stop hook이
에이전트 호출마다 발동해 결과에 git 메시지가 섞여 들어간다.
